---
listing: suppression
description: "Insert a // comment header at the top of files missing one. IDE0073. Defaults to `(not set)`"
---

<!-- generated by Nullean.Curb.OptionDocs -->

# `file_header_template`

Insert a // comment header at the top of files missing one. IDE0073.

## Values

| Value | Notes |
|---|---|
| `<template>` | Any integer. |
| `unset` |  |

**Default:** `(not set)`

### `file_header_template = unset`

```ini
file_header_template = unset
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double()
    {
        return Value * 2;
    }
}
```
:::

:::{tab-item} After
```csharp
// unset

namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double()
    {
        return Value * 2;
    }
}
```
:::

::::

