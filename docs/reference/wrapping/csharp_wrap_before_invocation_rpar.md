---
listing: wrapping
description: Put a broken argument list's ) on its own line. Defaults to `(not set)`.
---

<!-- generated by Nullean.Kerf.OptionDocs -->

# `csharp_wrap_before_invocation_rpar`

Put a broken argument list's ) on its own line.

## Values

| Value | Notes |
|---|---|
| `true` |  |
| `false` |  |

**Default:** `(not set)`

## Examples

The snippet below is formatted with default options and then again with the option applied.

**Snippet (formatted with defaults):**

```csharp
namespace N;

public class Widget
{
    public void Register(string name, int value, bool active, string tag)
    {
    }

    public void Run()
    {
        Register("kerf", 42, true, "formatter");
    }
}
```

### `csharp_wrap_before_invocation_rpar = true`

```ini
csharp_wrap_before_invocation_rpar = true
```

*No visible change on this snippet — the option affects other constructs.*

### `csharp_wrap_before_invocation_rpar = false`

```ini
csharp_wrap_before_invocation_rpar = false
```

*No visible change on this snippet — the option affects other constructs.*

