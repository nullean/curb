---
listing: wrapping
description: "Put a where clause on its own line. Defaults to `false`"
---

<!-- generated by Nullean.Curb.OptionDocs -->

# `csharp_wrap_before_first_type_parameter_constraint`

Put a where clause on its own line.

## Values

| Value | Notes |
|---|---|
| `true` |  |
| `false` |  |

**Default:** `false`

## Conformance

No known case where Curb's output is not a fixed point of `dotnet format` — see [conformance](../../design-principles/conformance.md).

### `csharp_wrap_before_first_type_parameter_constraint = true`

```ini
csharp_wrap_before_first_type_parameter_constraint = true
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    public T Max<T>(T a, T b) where T : IComparable<T>
    {
        return a.CompareTo(b) >= 0 ? a : b;
    }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{
    public T Max<T>(T a, T b)
        where T : IComparable<T>
    {
        return a.CompareTo(b) >= 0 ? a : b;
    }
}
```
:::

::::

### `csharp_wrap_before_first_type_parameter_constraint = false`

```ini
csharp_wrap_before_first_type_parameter_constraint = false
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    public T Max<T>(T a, T b) where T : IComparable<T>
    {
        return a.CompareTo(b) >= 0 ? a : b;
    }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{
    public T Max<T>(T a, T b) where T : IComparable<T>
    {
        return a.CompareTo(b) >= 0 ? a : b;
    }
}
```
:::

::::

