---
listing: expression-bodies
description: Convert eligible block-bodied local functions. IDE0061. Defaults to `false`.
---

<!-- generated by Nullean.Curb.OptionDocs -->

# `csharp_style_expression_bodied_local_functions`

Convert eligible block-bodied local functions. IDE0061.

## Values

| Value | Notes |
|---|---|
| `false` |  |
| `true` |  |
| `when_on_single_line` |  |

**Default:** `false`

## Example

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    public int Compute(int a, int b)
    {
        int Add(int x, int y) { return x + y; }
        return Add(a, b);
    }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{
    public int Compute(int a, int b)
    {
        int Add(int x, int y) => x + y;
        return Add(a, b);
    }
}
```
:::

::::

*Setting shown: `csharp_style_expression_bodied_local_functions = true`*

### `csharp_style_expression_bodied_local_functions = false`

```ini
csharp_style_expression_bodied_local_functions = false
```

```csharp
namespace N;

public class Widget
{
    public int Compute(int a, int b)
    {
        int Add(int x, int y) { return x + y; }
        return Add(a, b);
    }
}
```

