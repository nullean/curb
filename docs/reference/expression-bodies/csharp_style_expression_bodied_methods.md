---
listing: expression-bodies
description: "Convert eligible block-bodied methods to expression bodies. IDE0022. Defaults to `false`"
---

<!-- generated by Nullean.Curb.OptionDocs -->

# `csharp_style_expression_bodied_methods`

Convert eligible block-bodied methods to expression bodies. IDE0022.

## Values

| Value | Notes |
|---|---|
| `false` |  |
| `true` |  |
| `when_on_single_line` |  |

**Default:** `false`

## Conformance

No known case where Curb's output is not a fixed point of `dotnet format` — see [conformance](../../design-principles/conformance.md).

### `csharp_style_expression_bodied_methods = false`

```ini
csharp_style_expression_bodied_methods = false
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double() { return Value * 2; }

    public string Describe() { return $"Widget({Value})"; }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double() { return Value * 2; }

    public string Describe() { return $"Widget({Value})"; }
}
```
:::

::::

### `csharp_style_expression_bodied_methods = true`

```ini
csharp_style_expression_bodied_methods = true
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double() { return Value * 2; }

    public string Describe() { return $"Widget({Value})"; }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double() => Value * 2;

    public string Describe() => $"Widget({Value})";
}
```
:::

::::

### `csharp_style_expression_bodied_methods = when_on_single_line`

```ini
csharp_style_expression_bodied_methods = when_on_single_line
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double() { return Value * 2; }

    public string Describe() { return $"Widget({Value})"; }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{
    public int Value { get; set; }

    public int Double() => Value * 2;

    public string Describe() => $"Widget({Value})";
}
```
:::

::::

