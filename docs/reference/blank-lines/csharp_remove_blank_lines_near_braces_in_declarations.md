---
listing: blank-lines
description: "Force no blank line directly under a type/namespace's opening brace or above its closing one. Defaults to `true`"
---

<!-- generated by Nullean.Curb.OptionDocs -->

# `csharp_remove_blank_lines_near_braces_in_declarations`

Force no blank line directly under a type/namespace's opening brace or above its closing one.

## Values

| Value | Notes |
|---|---|
| `true` |  |
| `false` |  |

**Default:** `true`

## Conformance

No known case where Curb's output is not a fixed point of `dotnet format` — see [conformance](../../design-principles/conformance.md).

### `csharp_remove_blank_lines_near_braces_in_declarations = true`

```ini
csharp_remove_blank_lines_near_braces_in_declarations = true
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{

    private int _x;

    public void Run()
    {

        int a = 1;
        Console.WriteLine(a);
    }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{
    private int _x;

    public void Run()
    {
        int a = 1;
        Console.WriteLine(a);
    }
}
```
:::

::::

### `csharp_remove_blank_lines_near_braces_in_declarations = false`

```ini
csharp_remove_blank_lines_near_braces_in_declarations = false
```

::::{tab-set}

:::{tab-item} Before
```csharp
namespace N;

public class Widget
{
    private int _x;

    public void Run()
    {
        int a = 1;
        Console.WriteLine(a);
    }
}
```
:::

:::{tab-item} After
```csharp
namespace N;

public class Widget
{

    private int _x;

    public void Run()
    {
        int a = 1;
        Console.WriteLine(a);
    }
}
```
:::

::::

