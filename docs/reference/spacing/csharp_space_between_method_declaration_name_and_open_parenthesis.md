---
listing: spacing
description: Space between the method name and its opening parenthesis. Defaults to `false`.
---

<!-- generated by Nullean.Kerf.OptionDocs -->

# `csharp_space_between_method_declaration_name_and_open_parenthesis`

Space between the method name and its opening parenthesis.

## Values

| Value | Notes |
|---|---|
| `true` |  |
| `false` |  |

**Default:** `false`

## Examples

The snippet below is formatted with default options and then again with the option applied.

**Snippet (formatted with defaults):**

```csharp
namespace N;

public class Widget
{
    public void Register(string name, int value, bool active, string tag)
    {
        Console.WriteLine(name);
    }
}
```

### `csharp_space_between_method_declaration_name_and_open_parenthesis = true`

```ini
csharp_space_between_method_declaration_name_and_open_parenthesis = true
```

```csharp
namespace N;

public class Widget
{
    public void Register (string name, int value, bool active, string tag)
    {
        Console.WriteLine(name);
    }
}
```

### `csharp_space_between_method_declaration_name_and_open_parenthesis = false`

```ini
csharp_space_between_method_declaration_name_and_open_parenthesis = false
```

*No visible change on this snippet — the option affects other constructs.*

