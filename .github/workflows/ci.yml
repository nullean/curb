name: Always be deploying

on:
  pull_request:
    paths-ignore:
      - 'README.md'
      - 'NOTICE'
  push:
    paths-ignore:
      - 'README.md'
      - 'NOTICE'
    branches:
      - main
    tags:
      - "*.*.*"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}

permissions:
  contents: read

jobs:

  # ── AOT pack matrix ─────────────────────────────────────────────────────────
  # Native AOT can only compile for the OS/arch it runs on, so each leg compiles
  # its own RID, smoke-tests the binary, and packs the per-RID tool package.
  aot-pack:
    strategy:
      fail-fast: false   # a preempted arm runner must not cancel the whole matrix
      matrix:
        include:
          - rid: linux-x64
            runner: ubuntu-latest
          - rid: linux-arm64
            runner: ubuntu-24.04-arm
          - rid: win-x64
            runner: windows-latest
          - rid: win-arm64
            runner: windows-11-arm
          - rid: osx-arm64
            runner: macos-latest

    runs-on: ${{ matrix.runner }}
    name: AOT pack (${{ matrix.rid }})

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - run: git fetch --prune --tags
        shell: bash

      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      # Gate: the engine must link and run under AOT before anything gets packed.
      - name: AOT smoke test — ${{ matrix.rid }}
        shell: bash
        run: |
          dotnet publish examples/kerf-aot-smoketest -c Release -r ${{ matrix.rid }}
          EXT=""
          [[ "$RUNNER_OS" == "Windows" ]] && EXT=".exe"
          BINARY=".artifacts/publish/kerf-aot-smoketest/release_${{ matrix.rid }}/kerf-aot-smoketest${EXT}"
          OUTPUT=$("$BINARY")
          echo "$OUTPUT"
          echo "$OUTPUT" | grep -q "^OK:" || (echo "ERROR: AOT smoke test failed" && exit 1)

      # Packing WITH -r emits only this RID's package. The root package and the portable 'any'
      # fallback come from the coordinating build job instead — see generatePackages in
      # build/scripts/Targets.fs for why the per-RID packages must be built here, on a matching
      # runner, rather than all at once on one machine.
      - name: Pack native-AOT tool for ${{ matrix.rid }}
        run: dotnet pack src/Nullean.Kerf.Cli/Nullean.Kerf.Cli.csproj -c Release -r ${{ matrix.rid }} -o build/output
        shell: bash

      - name: Report native binary size
        shell: bash
        run: |
          EXT=""
          [[ "$RUNNER_OS" == "Windows" ]] && EXT=".exe"
          BINARY=$(find .artifacts -name "Nullean.Kerf.Cli${EXT}" -type f | head -1)
          [[ -n "$BINARY" ]] && ls -la "$BINARY" || echo "(no native binary found)"

      - name: Upload per-RID package
        if: github.event_name == 'push'
        uses: actions/upload-artifact@v7
        with:
          name: nupkg-${{ matrix.rid }}
          path: build/output/*.nupkg
          if-no-files-found: error

  # ── Corpus and conformance ──────────────────────────────────────────────────
  # Every bug this project has had was found here rather than by the unit tests:
  # dropped tokens, welded identifiers, indentation drift, non-idempotent output.
  # Until this ran in CI it gated nothing.
  corpus:
    runs-on: ubuntu-latest
    name: Corpus and conformance

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json

      - name: Check out the corpus
        run: git clone --depth 1 https://github.com/elastic/docs-builder.git /tmp/corpus

      - name: Build
        run: ./build.sh build -s true

      # Both verifiers on every file, and formatting must not fail or produce
      # source that no longer parses.
      - name: Verify the corpus formats safely
        run: |
          dotnet run --project src/Nullean.Kerf.Cli -c Release -- check /tmp/corpus | tee result.txt
          grep -qE '0 failed, 0 unparsable' result.txt \
            || (echo "::error::corpus reported failures" && exit 1)

      - name: Verify formatting is idempotent
        run: |
          cp -R /tmp/corpus /tmp/pass1
          dotnet run --project src/Nullean.Kerf.Cli -c Release -- format /tmp/pass1 >/dev/null
          cp -R /tmp/pass1 /tmp/pass2
          dotnet run --project src/Nullean.Kerf.Cli -c Release -- format /tmp/pass1 >/dev/null
          diff -rq /tmp/pass2 /tmp/pass1 \
            || (echo "::error::formatting is not idempotent" && exit 1)

      # The product claim, as a number that must not go down.
      #
      # Two files out of 1,196 differ, both the arrow-clause anchor recorded in the plan's §1d.
      - name: Conformance with dotnet format
        run: ./build.sh conformance -s true --corpus /tmp/corpus --minimum 100

      # The corpus sets max_line_length = 160, so --reflow is the *default* path for it: a width selects
      # deterministic layout as well as the column. Held at 100 rather than the 99.9 this gate used to
      # sit at, and that is the change rather than an oversight — deterministic layout has no source
      # arrangement for dotnet format to disagree with, so it is strictly cleaner than the preserving
      # reflow it replaced. If this ever drops, the default is not shippable however good its churn looks.
      - name: Conformance with a width (deterministic, the default)
        run: ./build.sh conformance -s true --corpus /tmp/corpus --reflow --minimum 100

      # At 120, one file differs: a property pattern whose brace dotnet format moves to its own line.
      # The same file the preserving gate below loses, so this is parity with a known held defect rather
      # than a new one — fixing it by keying on the source misses the case entirely, and restructuring
      # the clause to key on the printed layout cost two files elsewhere.
      - name: Conformance with a width of 120 columns
        run: ./build.sh conformance -s true --corpus /tmp/corpus --width 120 --minimum 99.9

      # The opt-out, which has to stay measurable now that a width no longer selects it. 99.92% — the
      # number the default used to be held to, on the same one file.
      - name: Conformance with the preservation opt-out
        run: ./build.sh conformance -s true --corpus /tmp/corpus --reflow --preserve --minimum 99.9

      # Idempotency is what the default now buys by construction rather than by measurement: layout is a
      # function of tokens and width, and tokens do not move. A failure here means something in the
      # printer is reading its own output back as intent.
      - name: Verify formatting with a width is idempotent
        run: |
          cp -R /tmp/corpus /tmp/det1
          dotnet run --project src/Nullean.Kerf.Cli -c Release -- format /tmp/det1 >/dev/null
          cp -R /tmp/det1 /tmp/det2
          dotnet run --project src/Nullean.Kerf.Cli -c Release -- format /tmp/det1 >/dev/null
          diff -rq /tmp/det2 /tmp/det1 \
            || (echo "::error::formatting is not idempotent with a width set" && exit 1)

      # Reported, not gated. Churn is the cost of adopting Kerf on a repository that already has a layout,
      # and it used to live in a docs paragraph as "742 of 1,196" with no way to re-derive it — while being
      # the stated reason for the largest design decision in the printer.
      #
      # Three configurations, because the pair is what makes either number mean anything, and because file
      # count alone is misleading here: the default and the opt-out differ by 150 files but by 2.7x in
      # changed lines. The no-width run is the "zero churn on an IDE0055-clean repository" claim, which the
      # README makes and which is worth keeping honest — it is 742 files of blank-line collapsing today.
      - name: Churn, all three configurations
        run: |
          ./build.sh churn -s true --corpus /tmp/corpus
          ./build.sh churn -s true --corpus /tmp/corpus --reflow
          ./build.sh churn -s true --corpus /tmp/corpus --reflow --preserve

      # Publishes native AOT and measures that, not the JIT build. On a run this short JIT
      # compilation dominates, so `dotnet run` reports several times the real cost and reads as a
      # regression that is not there. The gate is on allocations because they are deterministic;
      # wall time is reported for the trend but never asserted on a hosted runner.
      #
      # 31x rather than 30x since expression bodies landed, and the extra is verification rather
      # than printing. The corpus asks for the expression-body rules, so Kerf now rewrites 25 files
      # it used to leave alone and re-parses each one to prove the rewrite: 43 files take the second
      # parse where 18 did. Measured, on the same corpus and binary — 30.4x with verification on and
      # 28.9x with it off, against a 29.9x baseline whose own verified figure is higher than that.
      # The printing got no more expensive; there is simply more of it being checked.
      - name: Speed and allocations
        run: ./build.sh perf -s true --corpus /tmp/corpus --maxallocationratio 31

      # Two-sided on purpose. The sample builds clean with Kerf in the way even though IDE0055 is an
      # error and its source is misformatted, and fails with those errors when Kerf is bypassed. The
      # first assertion alone would pass just as well if the analysers were never running, which is
      # the shape this kind of test usually takes.
      - name: MSBuild integration runs before the compiler
        run: ./build.sh msbuildsmoketest -s true

      # Every expectation the suite asserts, written out and run through dotnet format. Until this
      # existed only the corpus proved the fixed-point property; the hand-written expectations proved
      # only that Kerf agrees with itself, so one written from a wrong belief would pass forever — it
      # found exactly that on its first run, an option value Roslyn rejects outright.
      #
      # A floor rather than zero: three disagree, and each is a judgement rather than a defect. Two
      # are tests feeding a deliberately invalid value, where Kerf keeps its default and Roslyn
      # declines to apply the option at all. The last has no resolution: Kerf leaves a
      # `#pragma warning disable` region alone and dotnet format ignores pragmas entirely, so that
      # region cannot be a fixed point either way.
      - name: Expectations survive dotnet format
        run: ./build.sh verifyexpectations -s true --minimum 99.5

  # ── Managed build / test / pack / publish ───────────────────────────────────
  build:
    runs-on: ubuntu-latest
    needs: aot-pack
    permissions:
      contents: write   # create GitHub release
      packages: write   # push to GitHub Packages
      issues: write     # release-notes creates labels for its categories

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # MinVer derives the version from tags, so the full tag list must be present.
      - run: git fetch --prune --tags

      - uses: actions/setup-dotnet@v5
        with:
          global-json-file: global.json
          source-url: https://nuget.pkg.github.com/nullean/index.json
        env:
          NUGET_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        run: ./build.sh build -s true

      - name: Test
        run: ./build.sh test -s true

      - name: Generate managed packages
        if: github.event_name == 'push'
        run: ./build.sh generatepackages -s true

      - name: Download all per-RID AOT packages
        if: github.event_name == 'push'
        uses: actions/download-artifact@v8
        with:
          pattern: nupkg-*
          path: build/output
          merge-multiple: true

      - name: Validate managed packages
        if: github.event_name == 'push'
        run: ./build.sh validatepackages -s true

      - name: Inspect public API changes
        if: github.event_name == 'push'
        run: ./build.sh generateapichanges -s true

      - name: Publish canary to GitHub Packages (branch push)
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/heads')
        shell: bash
        run: |
          until dotnet nuget push 'build/output/*.nupkg' \
            -k ${{ secrets.GITHUB_TOKEN }} \
            --skip-duplicate \
            --no-symbols; do echo "Retrying"; sleep 1; done

      - name: Generate release notes (tag push)
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
        run: ./build.sh generatereleasenotes -s true

      - name: Create GitHub release (tag push)
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
        run: ./build.sh createreleaseongithub -s true --token ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to nuget.org (tag push)
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags')
        run: |
          dotnet nuget push 'build/output/*.nupkg' \
            -k ${{ secrets.NUGET_ORG_API_KEY }} \
            -s https://api.nuget.org/v3/index.json \
            --skip-duplicate \
            --no-symbols
